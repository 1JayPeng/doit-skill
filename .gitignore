# Generated files
.code-review-graph/
.venv/
__pycache__/
*.py[oc]
build/
dist/output/
dist/*.tar.gz
wheels/
*.egg-info
.scratch/
.spec/
node_modules/

# IDE and tools
.cursorrules
.windsurfrules
.qoder/
.kiro/
.gemini/
.opencode.json
.python-version

# Skill installation (installed skills are project-local)
# .claude/skills/ is loaded by Claude Code — don't gitignore it
# But exclude the doit-skill dev repo's test install:
# (This repo's skills are at root, .claude/skills/ is for testing)
.claude/skills/

# Skill installation
doit-pack/

# opencode install artifacts (generated by setup.sh, source lives at commands/ and skills/)
.opencode/commands/
.opencode/skills/

# doit workflow runtime (auto-generated by setup.sh, per-user)
.doit/

# Local settings
settings.local.json
package-lock.json
.tokensave
.codegraph/

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json
.claude-plugin/
# Local dev config (doit-skill dev repo only — never push)
.agents/
.claude/
.local/
AGENTS.md
docs/
headroom_memory.db
skills-lock.json

# Backup files
*.bak
*.old
*.orig
